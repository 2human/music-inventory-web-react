const defaultState = {
  modal: undefined,
  modalOpen: false,
  dataType: undefined,
  dataId: undefined,
};

export const modalReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
